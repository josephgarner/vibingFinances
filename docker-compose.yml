version: '3.8'

services:
  vibe-finances-app:
    build: .
    ports:
      - "4029:3000"
    networks:
      - vibe_network
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./uploads:/app/uploads

networks:
  vibe_network:
    driver: bridge